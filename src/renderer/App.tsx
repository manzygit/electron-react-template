import { ReactElement } from "react";
import './App.scss';

export default function App(): ReactElement {
    return (
        <div className="app">
            <h1>Vite + React + Typescript</h1>
        </div>
    );
}